/**
 * @typedef {object} QueryMetadata
 * @property {object} LastEvaluatedKey
 */

/**
 * @template T
 * @typedef {object} PaginateQuery<T>
 * @property {Array<T>} data
 * @property {QueryMetadata} metadata
 */
