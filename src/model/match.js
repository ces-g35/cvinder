/**
 * @typedef {object} MatchView
 * @property {Id} id - id of the match itself
 * @property {Id} user_id - user which match with targetId (i.e. user fetch for target)
 * @property {Id} target_id - id of the target
 * @property {UserPair} user_pair - user pair
 * @property {string} target_name - user pair
 * @property {string} img_url - image url of targetId
 * @property {number} created_date - unix timestamp when they match
 */
